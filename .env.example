# Spotify API credentials for /api/spotify (the music card).
# Copy this file to ".env" and fill in real values. Do NOT commit ".env".
# For deployment, set these same vars in your Vercel project settings.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
